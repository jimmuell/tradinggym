# Lane B — headless Playwright QA run (2026-07-11)

Target: **Published** host `https://keen-chart-clone.lovable.app`. App-repo only. No SQL state edits.
Every Pass/finding carries an artifact (ran assertion, DB/network response, or screenshot). Where a
cause was not observed, it is labeled "not observed."

> **Update (Foundation-graduation VERIFY run):** the LRN-03 blocker below was **fixed** on `main` as
> `f3263d1` ("Fixed foundation progress") and **verified live on a brand-new account** — see the
> **Foundation graduation VERIFY** section. LRN-03 and the LRN-05 Foundation leg are now **Pass**.
>
> **Update 2 (Assessment VERIFY run, real 10-question):** the earlier graduation Pass cleared a
> **1-question stub** that had shadowed the gate May 9→Jul 11 (three published platform foundation
> quizzes; `useQuizByModule` takes `created_at DESC LIMIT 1`). The stubs are now unpublished and a
> partial unique index enforces one published quiz per module. Re-verified live: the gate now serves
> the real **10-question** assessment and graduation still holds — see **Assessment VERIFY** section.
> `foundation-gate.spec.ts` de-hardcoded (7→N-from-DB) and now green.
>
> **Update 3 (S-grad restore):** the "S-grad credential rotated" call in Update 2 was **wrong** and is
> corrected below (Retractions #1). S-grad was never broken — `TgymQA2026!` is stale documentation;
> the account uses the seed password (`TEST_PASSWORD` / `password123`), like every dev-login button.
> Test D restored to S-grad (Free-plan, Foundation-graduated) sourced from `TEST_PASSWORD`, plus a
> new `plan_state !== 'admin'` guard so it can't pass via the admin bypass. 5 passed.

| Test Case | Status | Evidence | Notes |
|-----------|--------|----------|-------|
| STR-03 | **Blocked (not executed)** | on-demand spec `e2e/str-extract-backtest.live.spec.ts` (live tier) | Metered; one manual run after review. Hardened earlier: removed all `.catch(() => {})`, added `toHaveValue('2024-01-01'/'2024-01-31')` guards before Run, tightened success asserts to real labels ("Entry Rules"/"Exit Rules" + ConfidenceBanner; "Total Trades" + "Engine v"). Not run here. |
| X-03 | **Pass** | `e2e/lane-b-batch2.spec.ts` ran green | Asserts the ACTUAL toast `Failed to start backtest` (Backtesting.tsx:79). Teeth proven (200 mock → `count = 0`). Quota-free. |
| LRN-03 | **Pass** (fix verified live; re-confirmed vs REAL 10-question assessment) | `e2e/foundation-graduation-fresh.spec.ts` green — 30.8s (stub run) **and** re-run vs the real assessment; server `tier_state='tier1'` + 5 `lesson_progress` rows | Brand-new `+qalrn` signup completed all **5** modules → "5 of 5 modules complete" → Take Assessment ENABLED → passed the server-graded assessment (`submit_quiz_attempt`) → risk-ack → `graduate_foundation` → Simulator/Strategies/Analytics unlocked + Tier 1 open; **survived a hard reload**. The prior Pass cleared a 1-question stub; now re-confirmed against the real **10-question** gate — see Assessment VERIFY. |
| LRN-04 | **Pass** (negative + tier1) / **Blocked** (tier2/3 open) | `e2e/lane-b-batch2.spec.ts` — 2 tests green | Un-graduated → Tier 1/2/3 locked with correct gates; Foundation-graduated (tier1) → Tier 1 open, Tier 2/3 gated. **Tier 2/3-OPEN half Blocked by design** (20 sessions + win-rate + step-accuracy gates; not scriptable, not SQL-forceable). |
| LRN-05 | **Foundation leg: Pass** (vs real 10-question assessment) / **Tier continuation: Blocked** | Foundation leg shares the Assessment VERIFY run (graduation from zero, 10 questions cleared) | Foundation graduation works end-to-end against the real gate (see LRN-03). Tier 1→2→3 continuation remains **Blocked** on the trading-performance gates (unchanged — same gates as LRN-04). |

## Foundation graduation VERIFY (positive half, from zero)

**Fix under test:** `f3263d1` — `Foundation.tsx` now derives the module count from
`useCompletedLessonIds()` (server `lesson_progress`) instead of `localStorage['completedLessons']`.
Both the write (`useMarkLessonComplete`) and the read (`useCompletedLessonIds`) hit `lesson_progress`.

**Bundle confirmation (precondition):** the fixed page issues `GET /rest/v1/lesson_progress?select=lesson_id…`
on load; the old bundle read localStorage and never made that request. The VERIFY spec waits for that
request before asserting — it fired, so the **new bundle is live**. (A pass is only possible on the
fixed bundle: on the old build a fresh account shows "0 of 5" and the spec fails at the count/enable
step.)

**New guard:** `e2e/foundation-graduation-fresh.spec.ts` — fresh signup → 5 modules → assessment →
graduate → hard reload. This is the POSITIVE half that never existed; `foundation-gate.spec.ts` is the
NEGATIVE half (gate rejects the unqualified). Complements. The positive half is the one whose absence
let the bug live Jul 7–11 (every account we tested was already graduated).

**Observed, artifact-backed:**
- Live Foundation lesson set = **5** (`f1_candles, f2_structure, f3_sessions, f4_risk, f5_plan`) — DB query.
- Fresh account: `tier_state='foundation'`, `lesson_progress` rows = 0 at start (authed supabase-js).
- Control (teeth baseline): before completing anything, "0 of 5 modules complete" + Take Assessment
  DISABLED, with `localStorage['completedLessons'] === null`.
- After completing 5 modules via UI: `lesson_progress` rows = 5 (DB), `localStorage` still null; removed
  `completedLessons*` keys and hard-reloaded → "5 of 5 modules complete" + Take Assessment ENABLED.
- Assessment passed via `submit_quiz_attempt` (server-graded) → PASSED badge + "1 / 1 correct"
  (at the time of THIS run the served assessment was a **1**-question stub — later found to be a
  shadow bug and fixed; re-verified against the real 10-question gate in **Assessment VERIFY** below).
- Risk-ack (`update_own_profile`) → `graduate_foundation` → **`tier_state='tier1'`** (server).
- Simulator canvas visible (no "Simulator Locked"); Strategies open ("Browse proven trading strategies");
  Analytics open (no "Analytics Locked"); Tier 1 open (no "Complete Foundation to unlock").
- Hard reload after graduation → "5 of 5" + gates still open + `tier_state='tier1'` (persisted).

**Teeth — proven empirically (not just by construction).** A throwaway variant (not committed)
completed all 5 modules for real (DB holds 5 rows), then intercepted only the `lesson_progress` GET →
`[]` and reloaded: the page collapsed to **"0 of 5 modules complete"** and re-disabled Take Assessment,
and "5 of 5" was absent. So the count tracks the DB read and nothing else — a non-DB source (the old
bug) could not produce the fresh-account "5 of 5", and the committed assertion goes red the moment the
DB read stops backing it. Run: `2 passed (17.3s)`; teeth account below.

## Assessment VERIFY (real 10-question gate)

**What was wrong:** three published platform quizzes existed for `module='foundation'`;
`useQuizByModule()` orders `created_at DESC LIMIT 1`, so from May 9→Jul 11 the app served a
**1-question** stub as the graduation gate (1 correct = 100% ≥ 80%). The real **10-question**
assessment (`06d7aff8…`, Apr 22) was buried, not gone. Fixed before this run: stubs unpublished
(not deleted — `quiz_attempts` reference them), and a partial unique index
`quizzes_one_published_platform_per_module` now blocks a duplicate publish. This run confirms the
**running app** serves the real assessment (data-right vs product-does-right).

**Observed, artifact-backed (`e2e/foundation-graduation-fresh.spec.ts`, live, green):**
- DB (same query as `useQuizByModule('foundation')`): served quiz `06d7aff8-85ce-41e9-81dc-88dad266a897`,
  **10 questions**, `pass_threshold = 80`; **exactly one** published platform foundation quiz.
- New shadow guards in the spec (both DB-derived, no hardcoded 10):
  - `expect(publishedQuizzes.length).toBe(1)` — defence-in-depth behind the DB index.
  - UI assertion `Question 1 of {expectedQuestionCount}` — the running app must render as many
    questions as the single published row; a stub leaking through goes red here.
- All **10** questions answered through the UI; `submit_quiz_attempt` (server-graded) returned
  PASSED with "10 / 10 correct" (≥ 80%); risk-ack → `graduate_foundation` → `tier_state='tier1'`;
  Simulator/Strategies/Analytics unlocked + Tier 1 open; **survived a hard reload**.
- Passed legitimately — RPC not bypassed, threshold not weakened.

## De-hardcode sweep (Task 2)
- **`foundation-gate.spec.ts` — fixed.** Was `expect(ids.length).toBe(7)` + text `0 of 7 modules
  complete` (red on `main`; live Foundation = 5). Now derives `N` from the DB (shared
  `foundationLessonIds()` helper, same query as `useFoundationLessons`) and asserts `0 of N`. All 4
  tests green (5 passed incl. setup, 22.6s).
- **Test D — S-grad restored (earlier "repoint to pro@gmail.com" was CORRECTED — see below).**
  Fixture is `jamesloganmueller+sgrad@gmail.com`, password from `TEST_PASSWORD`, plus a new assertion
  that the account is **not** admin (`plan_state !== 'admin'`) so D cannot pass through the
  `isUnlocked = isAdmin || …` bypass. Verified live: sgrad + `TEST_PASSWORD` → login OK,
  `tier_state='tier1'`, `plan_state='starter'` (Free plan, Foundation-graduated). 5 passed (24.2s).
- **Rest of `e2e/` swept.** Only other numeric count-assertions are `orb-guided-walkthrough.spec.ts`
  `checkedCount(...).toBe(0/1/5/6)`. Those are checkpoint counts against a **committed, stubbed
  fixture** (`e2e/fixtures/orb-guided-scenario.json` via `stubGuidedScenario`), deterministic by
  construction — not live-content drift. Left as-is (reviewed, legitimate).

## Retractions (prior-version findings that were inferences, not observations)
1. **"S-grad credential rotated / account needs re-pointing" — RETRACTED (full correction).**
   The *observation* was correct: `signInWithPassword(sgrad, `**`TgymQA2026!`**`)` → "Invalid login
   credentials", reproduced twice. The *conclusion* was wrong. **The account was never broken.** Jim
   logged straight in via the S-grad dev-login button on the published app: alive, **Free plan**,
   Foundation-graduated (Simulator/Strategies/Analytics unlocked, "Continue Price Action — ORB").
   Root cause: **`TgymQA2026!` is stale documentation** — since Jul 8 the account was folded into the
   dev-login system, and `Auth.tsx handleDevLogin` authenticates every dev button (S-grad included)
   with `password123`, i.e. the seed password = `TEST_PASSWORD`. The failing login was the spec using
   a password the account does not have. Verified live now: sgrad + `TEST_PASSWORD` → OK, `tier1`,
   `plan_state='starter'`; and `TEST_PASSWORD === 'password123'`. Fix: `S_GRAD` restored to sgrad
   sourced from `TEST_PASSWORD` (no hardcoded literal). **Lesson: read the code, not the doc** — the
   same stale-note failure mode that hid the one-question graduation gate for two months.

   > **HANDOFF CORRECTION:** S-grad's password is the **seed password** (`TEST_PASSWORD` / `password123`),
   > **NOT `TgymQA2026!`**. The session handoff and the Jul-7 run sheet both carry the wrong value and
   > need updating.
2. **"No throwaway accounts were created" — RETRACTED.** Signup succeeded; accounts were created
   (cleanup list below). The prior "signup checkbox-race" theory was wrong — signup works.

## Throwaway accounts created (for pre-launch cleanup)
Prior LRN-03 revision:
- `jamesloganmueller+qalrn1783790344530@gmail.com`
- `jamesloganmueller+qalrn1783790443566@gmail.com`
- `jamesloganmueller+qalrn1783790584229@gmail.com` (has `lesson_progress` rows)

Foundation-graduation VERIFY run:
- `jamesloganmueller+qalrn1783793738898@gmail.com` — full graduation test; now **`tier_state='tier1'`** (graduated).
- `jamesloganmueller+qalrnteeth1783793837612@gmail.com` — teeth proof; `foundation`, 5 `lesson_progress` rows, not graduated.

Assessment VERIFY run (real 10-question):
- `jamesloganmueller+qalrn1783795261103@gmail.com` — graduated through the real 10-question assessment; now **`tier_state='tier1'`**.

S-grad restore run (created with `TEST_PASSWORD`):
- `jamesloganmueller+qalrn1783796450015@gmail.com` — graduated through the real assessment; now **`tier_state='tier1'`**.

Password note: the `+qalrn*` throwaways listed above were created in earlier runs when the fresh-signup
spec set `TgymQA2026!`. That literal has since been removed — `foundation-graduation-fresh.spec.ts` now
sources the signup password from `TEST_PASSWORD` (the seed password), so accounts created going forward
use `TEST_PASSWORD`. Either way they are disposable and slated for deletion by email.
