# Lane B — headless Playwright QA run (2026-07-11)

Target: **Published** host `https://keen-chart-clone.lovable.app`. App-repo only. No SQL state edits,
no billing/tier writes, no metered calls in loops. Every Pass below has a ran-assertion or a
DB/DOM response; Blocked = precondition unreachable without a banned shortcut.

| Test Case | Status | Evidence | Notes |
|-----------|--------|----------|-------|
| STR-03 | **Blocked (not executed)** | on-demand spec `e2e/str-extract-backtest.live.spec.ts` written (live tier, `RUN_LIVE_BACKTESTS=true`) | Metered (AI extract + real backtest). Placed in the live tier for a single manual run; not executed this session, so no observed pass to report. Requires a Foundation-graduated Pro account (pro@gmail.com now qualifies, tier1). |
| X-03 | **Pass** | `e2e/lane-b-batch2.spec.ts` — assertion ran green (4 passed) | Extends the existing ErrorBoundary guard with a harder failure (hard 500, non-JSON body) + the "app stays usable" half: clear error surfaced, config panel intact, Run button recovers, body not blank. Quota-free (mocked). |
| LRN-03 | **Blocked (attempted)** | driver got: answer key fetchable (10-Q assessment, `correct_index`); grading server-authoritative via `submit_quiz_attempt` RPC (verified — prior client-write hole is closed) | Fresh-signup automation is **flaky**: the two agreement checkboxes → Sign-Up-enable timing intermittently leaves the button disabled, so a full signup→assessment→graduation run couldn't be completed reliably. Cannot substitute `starter@gmail.com` (graduating it destroys the S-fresh lock-test account, per the GU-11 run sheet), and the former S-grad account is now invalid (see New Bugs). No SQL shortcut taken. |
| LRN-04 | **Pass (negative + tier1) / Blocked (tier2-3 open)** | `e2e/lane-b-batch2.spec.ts` — 2 tests ran green | Verified: un-graduated account → Tier 1/2/3 all locked with correct progression gates ("Complete Foundation…", "Complete Tier 1…", "Complete Tier 2…"); Foundation-graduated (tier1, pro@gmail.com) → Tier 1 **open**, Tier 2/3 still gated. The **Tier 2/3 OPEN** half is Blocked by design — it needs 20 sim sessions + win-rate + step-accuracy gates that can't be earned in a script and mustn't be SQL-forced. |
| LRN-05 | **Foundation leg: Blocked (attempted) / Tier continuation: Blocked** | shares LRN-03 setup | Foundation leg = same status/blocker as LRN-03 (fresh-signup flakiness). Tier 1→2→3 continuation is gated on the trading-performance gates named in LRN-04 — not scriptable, not SQL-forceable. Not reported as a whole-case Pass. |

## New bugs / findings (not on the case list)
1. **S-grad test account is gone/invalid.** `jamesloganmueller+sgrad@gmail.com` now returns
   "Invalid login credentials" (worked earlier this session). This **breaks the committed
   `e2e/foundation-gate.spec.ts` (test D)**, which logs in as that account — that guard will now
   fail in CI. Needs the account restored, or the spec re-pointed to another tier1 account
   (pro@gmail.com is currently graduated to tier1).
2. **`pro@gmail.com` is `tier_state:'tier1'` but `plan_state:'pro'`** — used here as the legitimate
   tier1 account. (Confirms PRO-06 unlock from the prior batch.)
3. **Quiz grading is now server-authoritative** — `quiz_attempts` insert path goes through the
   `submit_quiz_attempt` RPC; the direct client-write of `passed=true` that was open last session is
   the correct thing to reject now. (Positive confirmation, not a bug.)

## Throwaway accounts created this run
- None persisted: the LRN-03 signup attempts all failed **before** the Sign-Up submit (button
  disabled), so no new accounts were created. (Prior sessions' `jamesloganmueller+qa*` accounts
  remain on the existing pre-launch cleanup list.)
