# Session Changelog — 2026-05-07 11:58 UTC → 2026-05-08 11:00 UTC

## Summary
Multi-day session focused on (1) UI polish across the Landing, Settings, Profile, and Pricing pages; (2) opening Guru directory browsing to Free users while gating enrollment behind Pro/Expert; (3) removing the legacy referral discount mechanic; and (4) fixing the Simulator tier gate including a series of follow-up loading-flash fixes that culminated in hoisting `TierProvider` to the app root.

---

## Pre-Session Context (changes referenced at session start)
- `src/components/dashboard/LearningProgressCard.tsx` — Dynamic button label ("Start Learning" → "Continue Your Foundation"); F1–F5 checklist with checkmarks.
- `src/pages/Learning.tsx` — Removed redundant `LearningProgressCard` from Learning Hub.
- `src/pages/Dashboard.tsx` — Removed `FinancialDisclaimer` and `FoundationLearningPath`; moved `HelpSheet` into the subscription card flex row.
- `src/components/learning/RiskAcknowledgmentModal.tsx` — New non-dismissible modal for Foundation graduation.
- `src/pages/learning/FoundationLesson.tsx` — Intercepts quiz pass; shows risk modal before promoting to Tier 1.
- **Database migration** — Added `risk_acknowledged_at` column to `profiles` table; updated `update_own_profile` RPC.
- `src/components/dashboard/LaunchSessionCard.tsx` — Hidden for free users (`starter`, non-admin); removed locked-state UI branch.

---

## 2026-05-07 12:39 UTC — Landing / Settings / Profile / Pricing polish
- `src/pages/Landing.tsx` — Removed periods from pricing headline & subhead.
- `src/pages/Profile.tsx` — Two-column card layout (`md:grid-cols-2 max-w-5xl`).
- `src/pages/Settings.tsx` — Two-column card layout (`md:grid-cols-2 max-w-5xl`).
- `src/pages/PricingPage.tsx` — Added Guru to `PLANS` array; 4-column grid (`md:grid-cols-2 lg:grid-cols-4`); reduced card padding (`p-8` → `p-6`); amber Guru styling/CTA; removed standalone Guru section and unused `GURU_PRICE_ID`.

## 2026-05-07 13:52 UTC — UI-3: Guru Enrollment Gating + Referral Discount Removal
- `supabase/functions/enroll-with-guru/index.ts` — Removed Stripe `createBalanceTransaction()` (first-month-free credit); referred enrollments now keep standard 20% commission and `discount_applied: false`.
- `src/pages/GurusDirectoryPage.tsx` — Removed Foundation lock screen; added upgrade banner for `starter` users; directory grid now visible to all authenticated users.
- `src/pages/GuruPublicProfilePage.tsx` — Removed "First month free" promo card and referred-enrollment toast; replaced disabled tooltip buttons with visible upgrade card (Free) and "Complete Foundation first" card (Foundation).
- `src/pages/ClassesPage.tsx` — Plan-aware empty state CTA ("View Plans" for Free, "Find a Guru" otherwise).

## 2026-05-07 14:00 UTC — UI-3-FIX: Simulator Tier Gate + Disclaimer Removal
- `src/pages/Simulator.tsx` — Added page-level tier gate using `canAccess('simulator')`; renders centered lock screen with `AppSidebar` intact and "Go to Foundation" CTA when locked. Removed `FinancialDisclaimer` import and JSX.
- `src/components/FinancialDisclaimer.tsx` — **Deleted** (no remaining references; risk acknowledgment now handled by Foundation graduation gate).

## 2026-05-07 14:18 UTC — UI-3-FIX-2: Simulator Loading Flash Fix
- `src/pages/Simulator.tsx` — Added `tierLoading` skeleton guard (with `AppSidebar` shell) before the lock screen check, preventing the lock screen from flashing for paid users while TierContext loaded.

## 2026-05-07 14:30 UTC — UI-3-FIX-3: Skip Skeleton When Cached
- `src/pages/Simulator.tsx` — Loading guard tightened to `tierLoading && !planState` so cached navigation from another page skips the skeleton entirely while hard refreshes still show it briefly.

## 2026-05-07 14:52 UTC — UI-3-FIX-4: Hoist TierProvider to App Level
- `src/App.tsx` — `TierProvider` hoisted to app root (inside `SettingsProvider`); removed from `LayoutRoute`, `/simulator`, and `/guru/apply` so a single shared instance serves the whole app and eliminates re-mount flashes.
- `src/pages/Simulator.tsx` — Removed `Skeleton` import and `planState` destructure; simplified loading guard to `if (tierLoading) return null` (imperceptible blank frame on hard refresh only).

---

## Files Modified This Session (deduped)
- `src/App.tsx`
- `src/pages/Landing.tsx`
- `src/pages/Profile.tsx`
- `src/pages/Settings.tsx`
- `src/pages/PricingPage.tsx`
- `src/pages/Simulator.tsx`
- `src/pages/GurusDirectoryPage.tsx`
- `src/pages/GuruPublicProfilePage.tsx`
- `src/pages/ClassesPage.tsx`
- `supabase/functions/enroll-with-guru/index.ts`

## Files Deleted
- `src/components/FinancialDisclaimer.tsx`

---

## How to start the next session's changelog
Open a new chat thread (or just say so in this one) and use the trigger phrase:

> **"Let's track all our changes this session"**

I'll create a fresh `CHANGELOG_<YYYY-MM-DD>.md` at the project root from message #1 of that session and append entries as we go.
